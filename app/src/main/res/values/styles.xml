<resources>

    <style name="MainTabLayout.TabText" parent="TextAppearance.Design.Tab">
        <item name="android:fontFamily">@font/qtj</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="ShareElementAppTheme" parent="Theme.Ksg">
        <!--允许启用窗口内容转换效果 （共享元素）-->
        <item name="android:windowContentTransitions">true</item>
    </style>

    <!-- 练习生页面 button -->
    <style name="TraineeBtn" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">5dp</item>
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="android:backgroundTint">@color/color_FF9800</item>
        <item name="android:textSize">15sp</item>
    </style>

    <!-- 播放器页面 button -->
    <style name="PlayerMenuBtn" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="android:backgroundTint">@color/color_FF9800</item>
        <item name="android:textSize">15sp</item>
    </style>

    <!-- 圆形图片 -->
    <style name="CircleImageStyle">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <!-- 圆角图片 -->
    <style name="RoundedImageStyle">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">10dp</item>
    </style>

</resources>
